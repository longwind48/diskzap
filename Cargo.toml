[package]
name = "diskzap"
version = "0.1.0"
edition = "2021"
description = "Fast, safe reclaimer of regenerable cache and build files. Reports by default, deletes only on --apply."
license = "MIT"
repository = "https://github.com/longwind48/diskzap"
readme = "README.md"
keywords = ["cache", "disk", "cleanup", "storage", "prune"]
categories = ["command-line-utilities", "filesystem"]

[[bin]]
name = "diskzap"
path = "src/main.rs"

[dependencies]
# Kept deliberately small so the whole tree stays auditable.
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
strip = true
panic = "abort"
